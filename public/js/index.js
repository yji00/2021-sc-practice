	var $main = $('.main-wrapper');
	var $slick = $main.find('.slide-wrap');
	$slick.slick();

	// slideMain();

  
	// function slideMain() {
	// 		// var $slick = $('.main-wrapper .slide-wrapper');
	// 		// var $btPrev = $('.main-wrapper .bt-slide.left');
	// 		// var $btNext = $('.main-wrapper .bt-slide.right');
	// 		var $main = $('.main-wrapper');
	// 		var $slick = $main.find('.slide-wrap');
	// 		var options = cloneObject(slick);
	// 		options.slidesToShow = 1;
	// 		options.dots = false;
	// 		// options.responsive.splice(0,2);
	// 		$slick.slick(options);
	// 	}
